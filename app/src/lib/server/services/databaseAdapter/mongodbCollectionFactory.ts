import { databaseDocUtil } from "$lib/server/utility/dbUtil"
import type { databaseAdapter } from "./type"
import type Collection from "mongodb"

async createMongoCollection(collectionString) = (collection: Collection){
    return{

    }: databaseAdapter
}